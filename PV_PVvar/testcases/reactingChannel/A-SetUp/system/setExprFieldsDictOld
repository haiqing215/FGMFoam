/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  v2206                                 |
|   \\  /    A nd           | Website:  www.openfoam.com                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      setExprFieldsDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //
expressions
(
    PV
    {
        field       PV;
        dimensions  [0 0 0 0 0 0 0];

//        constants
//        {
//        }

        variables
        (
            "PV1   = FINALPROGRESSVARIABLE"
            "delta = 8.75e-3"
            "xmid  = 3*delta"
            "dx    = 2e-3"
        );

        fieldMask
        #{
            (pos().x() > xmid-dx) && (pos().x() < xmid+dx)
        #};

        expression
        #{
            PV1/(2*dx)*pos().x() - PV1/(2*dx)*(xmid-dx)
        #};
    }
);
/*expressions
(
    U
    {
        field       U;
        dimensions  [0 1 -1 0 0 0 0];

//        constants
//        {
//        }

        variables
        (
            "delta     = 8.75e-3"
            "Ubar      = 10.0"
            "deviation = 15*(rand() - 0.5)"
        );

        condition
        #{
            (pos().x() > 0)
        #};

        expression
        #{
            vector
            (
                6*Ubar*(pos().y()/(2*delta)*(1 - pos().y()/(2*delta))) + deviation,
                deviation,
                deviation
            )
        #};
    }
);*/

// ************************************************************************* //
